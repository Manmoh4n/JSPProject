CREATE TABLE myschma.employee
(
    name character varying(250) NOT NULL,
    emp_id character varying(20) NOT NULL,
    designation character varying(100),
    address character varying(500),
    PRIMARY KEY (emp_id)
);


